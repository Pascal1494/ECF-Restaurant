{% extends 'base.html.twig' %}
{% block title %}Contactez nous
{% endblock %}
{% block body %}
	<div class="container">
		{# {{ form_start(form) }}
										{{ form_widget(form) }} #}

		{# {{ form_start(form) }}
				{{ form_row(form.name) }}
				{{ form_row(form.email) }}
				{# {{ form_row(form.subject) }} #}
		{# {{ form_row(form.message) }} #}
		{# {{ form_end(form) }} #}


		{# <button class="btn">{{ button_label|default('Envoyer') }}</button>
		
				{{ form_end(form) }} #}


		{{ form_start(form) }}
		{{ form_widget(form) }}<button class="btn"> {{ button_label|default('envoyer') }}</button>
		{{ form_end(form) }}
	</div>
{% endblock %}
