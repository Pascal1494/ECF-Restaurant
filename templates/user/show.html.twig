{% extends 'base.html.twig' %}

{% block title %} Edition du profil de {{user.firstname}}
{% endblock %}

{% block body %}
<div class="container">
	<h1>Bonjour {{app.user}}</h1>
	<table class="table">
		<tbody>
			{# <tr>
				<th>Id</th>
				<td>{{ user.id }}</td>
			</tr> #}
			<tr>
				<th>Email</th>
				<td>{{ user.email }}</td>
			</tr>
			{# <tr>
				<th>Roles</th>
				<td>{{ user.roles ? user.roles|json_encode : '' }}</td>
			</tr> #}
			{# <tr>
				<th>Password</th>
				<td>{{ user.password }}</td>
			</tr> #}
			{# <tr>
				<th>IsVerified</th>
				<td>{{ user.isVerified ? 'Yes' : 'No' }}</td>
			</tr> #}
			<tr>
				<th>Firstname</th>
				<td>{{ user.firstname }}</td>
			</tr>
			<tr>
				<th>Lastname</th>
				<td>{{ user.lastname }}</td>
			</tr>
			<tr>
				<th>Pseudo</th>
				<td>{{ user.pseudo }}</td>
			</tr>
			<tr>
				<th>Avatar</th>
				<td>{{ user.avatar }}</td>
			</tr>
			<tr>
				<th>Slug</th>
				<td>{{ user.slug }}</td>
			</tr>
		</tbody>
	</table>

	{# <a href="{{ path('app_user_index') }}">back to list</a> #}
	<button class="btn btn-purple"><a href="{{ path('app_user_edit', {'slug': user.slug}) }}">
		Mise à jour</a></button> 
	<button class="btn btn-purple"><a href="{{ path('app_home')}}">
		Retour sur le site</a></button> 

		<div class="mt-3">

	{{ include('user/_delete_form.html.twig') }}
		</div>
</div>
{% endblock %}
